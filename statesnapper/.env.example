# Pick one driver — pglite for dev/test, postgres for prod
DB_DRIVER=pglite

# PGLite path (file:// for persistent, memory:// for in-memory)
PGLITE_PATH=./local.pglite

# Postgres connection string — used when DB_DRIVER=postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/statesnapper
