---
phase: 05-full-text-search
plan: '02'
type: execute
wave: 2
depends_on:
- 05-01
files_modified:
- src/lib/chat/selection.ts
- src/lib/chat/__tests__/selection.test.ts
- src/lib/components/SearchBar.svelte
- src/lib/components/SearchResults.svelte
- src/lib/components/ChatView.svelte
- src/lib/components/MessageBubble.svelte
- src/routes/+page.svelte
- src/app.css
autonomous: true
requirements:
- SRCH-01
- SRCH-02
- SRCH-03

  truths:
  - A debounced search input in the app header searches every conversation and opens a results dropdown showing highlighted keywords, sender name, timestamp, conversation name, and the exact result count (SRCH-01, SRCH-03, SC-1, SC-3, SC-5)
  - A search input in the open conversation's header searches only that chat with the same results surface (SRCH-02, SC-2)
  - Clicking a result navigates (?chat= + ?at=) to that conversation scrolled to that specific message, with the target row visually flagged (SRCH-03, SC-4)
  - Deep links with ?at= survive reload; unknown or foreign message ids fall back to a normal newest-open with no error surface; plain ?chat= links still open pinned to newest (Phase 4 behavior unchanged)
  - All gates stay green: full Vitest suite, typecheck, svelte-check, Biome on touched dirs, static build, plus the manual browser pass on the dev origin (port 4647) with real IndexedDB
  artifacts:
  - src/lib/chat/selection.ts
  - src/lib/chat/__tests__/selection.test.ts
  - src/lib/components/SearchBar.svelte
  - src/lib/components/SearchResults.svelte
  - src/lib/components/ChatView.svelte
  - src/lib/components/MessageBubble.svelte
  - src/routes/+page.svelte
  - src/app.css
  key_links:
  - SearchBar/SearchResults consume 05-01's searchAll/searchChat/splitByTerms/makeSnippet verbatim — no query-shape logic in components
  - ?at= param follows the strict positive-safe-integer codec pattern of parseChatParam; message ids are only used as Dexie primary-key gets (getWindowAt validates chatId ownership)
  - ChatView seek uses getWindowAt(chatId, highlightId, PAGE_SIZE * 2) + seedWindow, then scrolls the data-msg-id row with scrollIntoView({ block: 'start' }) — the h-screen shell keeps the chat pane the only scroll container
  - Escaped interpolation only for every rendered archive string; {@html} stays banned on all chat/search content (T-04-01 precedent)


<objective>
Prove the search UI and result navigation end-to-end on top of the 05-01 query layer: header-wide and per-chat debounced search bars with highlighted, counted results; click-to-navigate into a conversation scrolled to the exact message via a ?at= deep link; and the plain ?chat= path (pinned-to-newest) untouched.

Purpose: Every SC-1..5 acceptance criterion of Phase 5 lands in this wave. The results dropdown is the single results surface for both modes; navigation reuses the Phase 4 windowing state machine with a seeded window instead of a second scroll implementation.
Output: Working search UI (global + per-chat) with count, highlight, and deep-linkable scroll-to-message navigation, verified against the real browser surface on port 4647.
</objective>

<execution_context>
@/Volumes/Main/GitHub/vibecoding/ragcgat/.opencode/gsd-core/workflows/execute-plan.md
@/Volumes/Main/GitHub/vibecoding/ragcgat/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-full-text-search/05-01-PLAN.md
@src/routes/+page.svelte
@src/lib/components/ChatView.svelte
@src/lib/components/ChatSidebar.svelte
@src/lib/components/MessageBubble.svelte
@src/lib/chat/selection.ts
@src/lib/chat/formatting.ts
@src/lib/chat/windows.ts
@src/lib/search/search.ts
@src/lib/search/highlight.ts
@src/lib/db/repositories.ts
@src/app.css
@package.json
</context>

<tasks>

<task type="tracer">
  <name>Tracer slice: ?at= codec, search components, header + chat-header wiring, seek-on-open navigation</name>
  <files>src/lib/chat/selection.ts, src/lib/components/SearchBar.svelte, src/lib/components/SearchResults.svelte, src/lib/components/ChatView.svelte, src/lib/components/MessageBubble.svelte, src/routes/+page.svelte, src/app.css</files>
  <read_first>.planning/phases/05-full-text-search/05-01-PLAN.md for the exact searchMessages/getWindowAt/seedWindow/splitByTerms/makeSnippet/searchAll/searchChat contracts the UI consumes; src/routes/+page.svelte for the header layout, tab state, selectedChat derivation, openChat/addPage goto pattern, and ChatView invocation; src/lib/components/ChatView.svelte for the $effect window load, scrollEl/sentinelEl bindings, loadOlder anchoring, and the pinned-to-newest open path; src/lib/components/MessageBubble.svelte for the two root row divs (event row and bubble row) that need data-msg-id; src/lib/components/ChatSidebar.svelte for the sidebar-scroller gotcha (the first .overflow-y-auto in the DOM is the sidebar aside — the chat scroller must be located via [data-msg] rows); src/lib/chat/selection.ts for the chat-param codec to mirror; src/lib/chat/formatting.ts for formatClock/senderColor; src/app.css for the dark-mode and animation conventions; @package.json for gates.</read_first>
  <action>Zero new dependencies (bun install must not be run). URL codec in src/lib/chat/selection.ts: ADD parseMessageParam(raw: string | null): number | null and serializeMessageParam(id: number): string mirroring the chat codec exactly — /^\d+$/ then Number.isSafeInteger && > 0, else null. A ?at= without a valid ?chat= is inert (ChatView only seeks when a chat id is present).

  src/lib/components/SearchResults.svelte (new): props { results: SearchResult[]; total: number; query: string; mode: 'global' | 'chat' }. Renders the count line ("N results for 'query'"), then up to the passed results (caller caps at 8): per result, chatName when mode === 'global', sender via senderColor, formatClock(message.timestamp), and the snippet = makeSnippet(message.text, tokenize(query)) re-split by splitByTerms with <mark> for match segments. Escaped interpolation only — {@html} forbidden; mark inherits the theme (Tailwind dark: variants). Clicking a result row calls onselect(message). Empty results → "No results for 'query'." line.

  src/lib/components/SearchBar.svelte (new): props { mode: 'global' | 'chat'; chatId?: number; onselect: (message: MessageRecord) => void }. Internal $state: query, results, total, open, loading, searchSeq = 0. Debounce 300ms in a $effect over query with setTimeout + cleanup (never before browser guard; searchAll/searchChat touched only when open input is non-empty after trim). tokenize(query).length === 0 → clear and close. On each fired search, increment searchSeq (monotonic); when the async result resolves, only store results/total/open if seq matches current — prevents a slow first query from overwriting a newer query's results. Run searchAll (global) or searchChat(db, chatId) (in-chat) with limit 8; store results/total; open = true. Esc key closes; click-outside closes via a document pointerdown listener added/removed in an $effect tied to open. input aria-label: 'Search all chats' / 'Search this chat'; dropdown nav role semantics: aria-expanded, role=listbox on the results container. Input clears on chat change (mode/chatId change resets query/results/open).

  src/routes/+page.svelte: header gains <SearchBar mode="global"> between the h1 and nav (hidden while on the import tab is fine — the header spans both tabs; SC-1 says 'search bar in the app header', so render it in the header: it navigates to browse). onselect handler: openChatAt(message.chatId, message.id). ADD openChatAt(chatId: number, msgId: number): tab = 'browse'; void goto(`?chat=${serializeChatParam(chatId)}&at=${serializeMessageParam(msgId)}`, { noScroll: true, keepFocus: true }). Derive selectedMsgId = parseMessageParam(page.url.searchParams.get('at')); pass highlightId={selectedMsgId} and onselectMessage={openChatAt} to ChatView. openChat (existing sidebar path) stays ?chat-only (clears any stale ?at= via the goto). RISK-3 fix: when openChat receives the same id as selectedId (already-open chat), it must still strip a stale ?at= param — openChat currently early-returns on id === selectedId; change to always goto '?chat='+id (stripping at) when id === selectedId and page.url.searchParams.has('at').

  src/lib/components/ChatView.svelte: ADD props highlightId?: number | null and onselectMessage?: (chatId: number, msgId: number) => void (NIT-3: pinned prop name). In the existing chat-load $effect: when typeof highlightId === 'number', load via const found = await messages.getWindowAt(chatId, highlightId, PAGE_SIZE * 2); when found → win = seedWindow(chatId, found.messages); assertRenderBudget; else fall back to the current getLatestWindow pinned-to-newest path and console.warn once (unknown/foreign message id — no user-facing error). After the flush tick in the seek branch, scroll: scrollEl?.querySelector(`[data-msg-id="${highlightId}"]`)?.scrollIntoView({ block: 'start' }) and set a transient highlight on the target row (state highlightIdActive — cleared on the next chat change; the flash ring animates once via CSS). The hasMore/loadOlder/trim path is unchanged (cursorOf reads the seeded pages[0][0]). ADD an in-chat <SearchBar mode="chat" chatId={chat.id} onselect={(msg) => onselectMessage?.(chat.id, msg.id)}> line under the chat header (only when chat is open); when onselectMessage is not provided, fall back to a local goto call.

  src/lib/components/MessageBubble.svelte: ADD data-msg-id={message.id} to both root divs (event row and bubble row). ADD optional prop highlight = false; when true, add a ring class to the bubble/event container (Tailwind ring + the app.css flash animation).

  src/app.css: ADD a one-shot flash keyframe (e.g. @keyframes search-flash: background/ring fade over ~1.2s with animation-fill-mode forwards on the highlight class, scoped to the data-highlight row); respect dark: variants.
  </action>
  <verify>
    <automated>bun run test</automated>
    <automated>bun run typecheck</automated>
    <automated>bunx biome check src/lib/components src/lib/chat/selection.ts src/lib/chat/__tests__/selection.test.ts src/routes src/app.css</automated>
    <automated>bun run check</automated>
    <automated>bun run build</automated>
  </verify>
  <done>Global and per-chat search bars render results with highlight, sender, time, chat name, and count; clicking a result opens the chat scrolled to that message with a flash cue; ?chat= deep links still pin to newest; unknown ?at= falls back quietly; all automated gates green.</done>
  <acceptance_criteria>Header search input searches across chats (chats imported on the dev origin at 4647 — re-import the Road Trip fixture); in-chat search scopes to the open chat; results show highlighted terms, sender name, clock time, and chat name (global) plus the "N results for 'q'" line; clicking a result navigates to the chat scrolled to the target row; reload of the ?chat=&at= URL re-opens scrolled to the same row; a fabricated ?at=999999 falls back to newest-open with no error; Esc and click-outside close the dropdown; sidebar ?chat= selection clears the at param; theme toggle and empty states unaffected; bun run typecheck / bunx biome check / bun run check / bun run build all exit 0.</acceptance_criteria>
</task>

<task type="auto">
  <name>Unit tests for the ?at= codec</name>
  <files>src/lib/chat/__tests__/selection.test.ts</files>
  <read_first>src/lib/chat/selection.ts created/updated by the tracer task for the exact parseMessageParam/serializeMessageParam exports; the existing chat-param tests for the convention (round-trip, reject non-numeric/zero/negative/oversized, null passthrough).</read_first>
  <action>Mirror the chat-param test block for message params: serializeMessageParam/parseMessageParam round-trip; parseMessageParam rejects non-numeric, zero, negative, and beyond-Number.MAX_SAFE_INTEGER values; null passthrough. Keep plain describe/it/expect, no DOM. (Windows seedWindow tests already landed in 05-01; component behavior is covered by the manual browser pass.)</action>
  <verify>
    <automated>bun run test -- src/lib/chat/__tests__/selection.test.ts</automated>
    <automated>bunx biome check src/lib/chat/__tests__/selection.test.ts</automated>
  </verify>
  <done>Message-param codec fully covered and Biome-clean.</done>
  <acceptance_criteria>Every case in the action passes; biome check exits 0.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query string to SearchBar state | User-typed text crosses into debounced searchAll/searchChat calls |
| ?chat= / ?at= query params to ChatView window reads | User- and link-controlled ids cross into getWindowAt/getLatestWindow |
| archive text to rendered results and bubbles | Message/sender/chat-name content crosses into the search and chat surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Cross-site scripting | search results (snippet, sender, chat name) and flagged message rows | high | mitigate | splitByTerms/makeSnippet return plain text; every rendered value is escaped interpolation; {@html} banned in SearchBar/SearchResults/MessageBubble (T-04-01 precedent) |
| T-05-02 | Tampering | ?chat= / ?at= selection params | medium | mitigate | Strict positive-safe-integer codecs (parseChatParam/parseMessageParam); ?at= ids used only as Dexie primary-key gets; getWindowAt validates chatId ownership and returns null → newest-open fallback with a console.warn, never an error surface |
| T-05-03 | Denial of service | unbounded search reads from the UI | medium | mitigate | Debounce 300ms; results capped at 8 rendered rows; limit 8 to the query layer (05-01 caps candidate fetch at 200 and counts via primaryKeys only); dropdown closed on empty/token-less queries |
| T-05-05 | Tampering | dependency supply chain | high | mitigate | Zero new dependencies in this plan (bun install must not run) |
</threat_model>

<verification>
bun run test -- src/lib/chat/__tests__/selection.test.ts passes; bun run typecheck exits 0; bunx biome check exits 0 on all touched dirs; bun run check exits 0; bun run build exits 0 with servable output. Manual browser pass on the dev origin (localhost:4647, real IndexedDB): re-import the Road Trip fixture (/tmp/Road Trip Plans.txt — well under the harness size limit), then verify (1) header search finds cross-chat matches with highlighted terms, sender, time, chat name, and count line; (2) in-chat search finds only that chat's matches; (3) clicking a result opens that conversation scrolled to the target message with the flash cue; (4) reload of the ?chat=&at= URL re-opens to the same row; (5) ?chat=1&at=999999 falls back to newest-open with no error; (6) Esc and click-outside close the dropdown; (7) sidebar selection clears the at param and pins to newest; (8) theme toggle + persistence still work. Follow the session's browser-harness rules: shim requestAnimationFrame on fresh tabs, locate the chat scroller via [data-msg] rows (never the first .overflow-y-auto — that is the sidebar), keep evaluate args small.
</verification>

<success_criteria>
SRCH-01 true (UI): header search bar returns cross-chat results with highlight, sender, time, chat name, and count. SRCH-02 true (UI): per-chat search bar returns only the open chat's matches. SRCH-03 true: results carry context snippets and clicking navigates to the conversation scrolled to that specific message, deep-linkable via ?at=. SC-1..SC-5 all true: header search, in-chat search, highlighted results with sender/time/chat name, scroll-to-message navigation, and the "N results for" count line. Phase 4 behaviors (pinned-to-newest open, sidebar order, theme, deep links) unchanged. All gates green.
</success_criteria>

<output>
Create `.planning/phases/05-full-text-search/05-02-SUMMARY.md` when done
</output>