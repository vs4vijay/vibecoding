---
phase: 01-parser-library
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/lib/parser/classify.ts
  - src/lib/parser/parseFile.ts
  - src/lib/parser/__tests__/classify.test.ts
  - src/lib/parser/__tests__/parser.test.ts
autonomous: true
requirements: [IMPR-04]

must_haves:
  truths:
    - "All 6 behavior cases pass: multiline grouping, system event detection, media classification, deleted message detection, call detection, participant extraction — including on ordinary prose"
    - "Ordinary prose containing 'left', 'added', 'removed' stays type text with real sender (word-boundary/full-phrase system matching, no silent sender drop)"
    - "Prose mentioning IMG-/VID-/AUD- prefixes or bare extensions stays type text; only full WhatsApp attachment filename shapes classify as media"
    - "'You deleted this message' classifies as deleted"
    - "The 'system' sentinel never appears in Chat.participants"
  artifacts:
    - "src/lib/parser/classify.ts (hardened system/media/deleted matchers)"
    - "src/lib/parser/parseFile.ts (participants excludes 'system' sentinel)"
    - "src/lib/parser/__tests__/classify.test.ts (regression tests for CR-01/WR-03/WR-04/IN-01)"
    - "src/lib/parser/__tests__/parser.test.ts (end-to-end parseString regression tests)"
  key_links:
    - "buildMessage() -> classifyMessage() classification correct on prose input"
    - "buildChat() participants contains only real senders"
---

<objective>
Close the 4 verified classification defects blocking Phase 1 (per D-gap set in VERIFICATION/REVIEW): system false-positives on prose, media false-positives on prose, missing deleted variant, 'system' polluting participants.

Purpose: Restore the phase goal's "reliably parsed" contract — ordinary conversational prose must not be re-tagged as system/media, and participants must contain only real senders.
Output: Hardened classify.ts + parseFile.ts, regression tests, green full suite, 24/24 benchmark still passing.
</objective>

<execution_context>
@/Volumes/Main/GitHub/vibecoding/skills/.opencode/gsd-core/workflows/execute-plan.md
@/Volumes/Main/GitHub/vibecoding/skills/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-library/01-VERIFICATION.md
@.planning/phases/01-parser-library/01-REVIEW.md
@src/lib/parser/classify.ts
@src/lib/parser/parseFile.ts
@src/lib/parser/__tests__/classify.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden classify.ts system/media/deleted matchers + regression tests</name>
  <files>src/lib/parser/classify.ts, src/lib/parser/__tests__/classify.test.ts, src/lib/parser/__tests__/parser.test.ts</files>
  <read_first>
    - src/lib/parser/classify.ts (current systemPatterns includes() loop lines 15-33, mediaPatterns regex list lines 35-53, deleted check line 61)
    - src/lib/parser/__tests__/classify.test.ts (12 existing tests — all must stay green)
    - .planning/phases/01-parser-library/01-VERIFICATION.md Behavioral Spot-Checks (exact repro strings)
    - .planning/phases/01-parser-library/01-REVIEW.md CR-01/WR-03/WR-04/IN-01 (suggested matcher shapes)
  </read_first>
  <action>Harded src/lib/parser/classify.ts without touching timestamp, normalization, dedup, call, or date logic (WR-02 date heuristic explicitly out of scope — do not change resolveDate). System matching: keep the null/empty-sender early return to system unchanged; replace the bare-substring entries 'left', 'added', 'removed' in systemPatterns with word-boundary or full-phrase matchers so ordinary prose no longer hits — implement as RegExp entries tested with .test() alongside the existing string entries (e.g. patterns matching standalone words or canonical system phrasings such as a sender name followed by left/joined/was added/was removed at end of body), while keeping all long-phrase entries ("joined using this group's invite link", "joined using invite link", "changed the group", "changed this group's icon", "security code changed", "messages and calls are end-to-end encrypted", "created group", "changed the subject", "changed this group's subject") matching as before; a real-sender message reading as ordinary prose must never return system. Media matching: tighten the IMG-/VID-/AUD- prefix regexes to require the full WhatsApp attachment filename shape (prefix followed by date-digit run and WA token, e.g. IMG-YYYYMMDD-WA with numeric suffix and extension) instead of bare prefix substrings, and anchor bare extension matches to filename tokens (word characters immediately before the dot and a filename-like token, not a bare space-dot or prose mention) so that the documented true positives still pass (IMG-20240709-WA0001.jpg to media/image, VID-20240709-WA0001.mp4 to media/video, AUD-20240709-WA0001.ogg to media/audio, document.pdf to media/document, <Media omitted> variants, sticker/gif/call/deleted existing cases) while the documented false positives stay text ("The IMG- tag is used for figures", "Check out this .gif url"). Keep <Media omitted>, image/video/audio/document omitted, sticker, GIF omitted, voice message, and call patterns untouched. Deleted matching: add the 'you deleted this message' lowercase variant alongside the two existing variants. Add regression tests to classify.test.ts for every reproduced failure (left/added/removed prose stays text, IMG- prose stays text, bare .gif prose stays text, You deleted this message is deleted, join invite link still system) plus end-to-end parseString tests in parser.test.ts asserting parseString of a sender line with "I left my keys at home" yields type text and sender Alice. Use bun and existing vitest imports; follow existing test style.</action>
  <verify>
    <automated>bun vitest run src/lib/parser/__tests__/classify.test.ts src/lib/parser/__tests__/parser.test.ts</automated>
  </verify>
  <acceptance_criteria>classifyMessage('I left my keys at home','Alice',...) returns type text (not system); classifyMessage('The IMG- tag is used for figures','Alice',...) returns type text (not media); classifyMessage('Check out this .gif url','Alice',...) returns type text (not media); classifyMessage('You deleted this message','Alice',...) returns type deleted; parseString('[2024/07/09, 08:01:49] Alice: I left my keys at home') message has type text and sender Alice; all 12 pre-existing classify tests still pass; bun vitest run on the two test files exits 0.</acceptance_criteria>
  <done>System/media/deleted matchers hardened, regression tests added, existing tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Exclude system sentinel from participants + full verification</name>
  <files>src/lib/parser/parseFile.ts, src/lib/parser/__tests__/parser.test.ts</files>
  <read_first>
    - src/lib/parser/parseFile.ts (buildMessage lines 108-137 finalSender assignment line 120, buildChat line 140 participants computation)
    - src/lib/parser/__tests__/parser.test.ts (existing participants/end-to-end tests)
    - .planning/phases/01-parser-library/01-VERIFICATION.md gaps missing item 4 and spot-check row for participants
  </read_first>
  <action>In src/lib/parser/parseFile.ts buildChat only, change the participants computation to filter out the literal 'system' sentinel in addition to falsy values (filter predicate rejects empty values and any sender strictly equal to 'system'); do not change buildMessage finalSender assignment, preamble handling, continuation caps, or resolveDate (WR-02 and IN-02 explicitly out of scope). Add a parser.test.ts regression test parsing a two-header input with one system event line plus one Alice sender line and asserting participants deep-equals ['Alice'] with no 'system' entry. Then run full verification: bun tsc --noEmit, bun vitest run (full suite, all 95+ pre-existing plus new tests), bunx biome check src/, and the 24-fixture benchmark (fixtures.test.ts 24/24 within the full run).</action>
  <verify>
    <automated>bun tsc --noEmit && bun vitest run && bunx biome check src/</automated>
  </verify>
  <acceptance_criteria>parseString of a chat with one system event plus one Alice message yields participants exactly ['Alice'] (no 'system' string present); bun tsc --noEmit exits 0; bun vitest run exits 0 with all tests passing including fixtures.test.ts 24/24; bunx biome check src/ reports 0 errors.</acceptance_criteria>
  <done>Participants exclusion fixed, full suite and benchmark green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export-file→parser | Untrusted WhatsApp .txt content crosses here; crafted prose must not corrupt classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | classify.ts system matcher | high | mitigate | Word-boundary/full-phrase system matching; never drop real sender attribution on prose |
| T-01-02 | Tampering | classify.ts media matcher | medium | mitigate | Full attachment filename shape + filename-token-anchored extension matches |
| T-01-SC | Tampering | npm installs | high | mitigate | No new packages in this plan; bun-only using existing toolchain |
</threat_model>

<verification>
- bun tsc --noEmit exits 0
- bun vitest run exits 0 (full suite: all pre-existing tests plus new regression tests; fixtures.test.ts 24/24)
- bunx biome check src/ reports 0 errors
- Spot-checks: "I left my keys at home" is text/Alice; "The IMG- tag is used for figures" is text; "Check out this .gif url" is text; "You deleted this message" is deleted; participants exclude 'system'
</verification>

<success_criteria>
- [ ] CR-01 fixed: left/added/removed prose stays text with real sender
- [ ] WR-03/IN-01 fixed: IMG-/.gif prose stays text, true attachment filenames still media
- [ ] WR-04 fixed: You deleted this message is deleted
- [ ] WR-01 fixed: participants excludes 'system' sentinel
- [ ] All pre-existing tests green, 24/24 benchmark passing, tsc + biome clean
- [ ] No date-resolution (WR-02), preamble-hash (IN-02), timestamp, storage, UI, or worker changes
</success_criteria>

<output>
Create `.planning/phases/01-parser-library/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/lib/parser/classify.ts` — MODIFIED: hardened system matchers (word-boundary/full-phrase for left/added/removed), media matchers (full attachment filename shape + filename-token-anchored extensions), added 'you deleted this message' deleted variant; exports `classifyMessage`, `getClassificationLabel` unchanged in signature
- `src/lib/parser/parseFile.ts` — MODIFIED: `buildChat` participants filter excludes 'system' sentinel; exports `parseString`, `buildMessage`, `buildChat`, `detectCapWarning` unchanged in signature
- `src/lib/parser/__tests__/classify.test.ts` — MODIFIED: new regression tests for CR-01/WR-03/WR-04/IN-01 repro cases
- `src/lib/parser/__tests__/parser.test.ts` — MODIFIED: new end-to-end parseString regression tests (prose sender attribution + participants exclusion)
