---
phase: 03-import-feature
plan: '02'
type: execute
wave: 2
depends_on:
- 03-01
files_modified:
- src/lib/import/unzip.ts
- src/lib/import/preview.ts
- src/lib/import/workerProtocol.ts
- src/lib/import/parse.worker.ts
- src/routes/+page.svelte
- src/lib/components/DropZone.svelte
- src/lib/components/PreviewCard.svelte
- src/lib/components/ProgressBar.svelte
- src/lib/import/__tests__/unzip.test.ts
- src/lib/import/__tests__/upsert.test.ts
- src/lib/import/__tests__/workerLogic.test.ts
- src/lib/import/__tests__/importState.test.ts
autonomous: true
requirements:
- IMPR-02
- IMPR-03
- IMPR-05
must_haves:
  truths:
  - User can file-pick or drag-drop a .zip containing a WhatsApp export and the largest .txt entry is extracted with zip-bomb caps enforced before parsing
  - Preview shows new-vs-skipped message counts by diffing parsed hashes against the existing IndexedDB via batched keyed queries, plus editable chat name with diff re-run on rename
  - Determinate progress is visible during worker parse and chunked store, with spinner under 100ms on drop and a progress bar during parse and store
  - Cancel discards in-memory state with zero writes; corrupt zips, missing-txt zips, and unparseable inputs surface user-facing errors with zero writes
  - Full suite plus lint plus static build stay green after extraction into components
  artifacts:
  - src/lib/import/unzip.ts
  - src/lib/components/DropZone.svelte
  - src/lib/components/PreviewCard.svelte
  - src/lib/components/ProgressBar.svelte
  - src/lib/import/__tests__/unzip.test.ts
  - src/lib/import/__tests__/upsert.test.ts
  key_links:
  - unzip.ts feeds raw entry bytes into the existing 03-01 worker path unchanged; zip listing stays on the main thread and only the parse runs in the worker
  - diffPreview uses batched dedupHash anyOf reads at 5000 keys per batch, never a full-store scan, and commit reuses the 03-01 commitImport unchanged
  - +page.svelte orchestrates status across DropZone, PreviewCard, and ProgressBar; components are thin bindings over tested logic in src/lib/import
---

<objective>
Expand the 03-01 tracer into the full import feature: .zip extraction with bomb guards, upsert new-vs-skipped diff with editable chat name, determinate progress wiring from worker events plus bulkSave onProgress, component extraction with error states, and full test coverage — closing every remaining Phase 3 success criterion.

Purpose: Deliver the complete preview-before-commit import experience for both .txt and .zip so Phase 4 has populated data to browse.
Output: Full-featured import page with zip support, upsert counts, progress bar, and green suite plus build.
</objective>

<execution_context>
@/Volumes/Main/GitHub/vibecoding/skills/.opencode/gsd-core/workflows/execute-plan.md
@/Volumes/Main/GitHub/vibecoding/skills/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-import-feature/01-RESEARCH.md
@.planning/phases/03-import-feature/03-01-SUMMARY.md
@src/lib/import/workerProtocol.ts
@src/lib/import/validate.ts
@src/lib/import/preview.ts
@src/lib/import/parse.worker.ts
@src/lib/import/commit.ts
@src/lib/db/repositories.ts
@src/routes/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Zip extraction plus upsert diff plus worker progress wiring</name>
  <files>src/lib/import/unzip.ts, src/lib/import/preview.ts, src/lib/import/workerProtocol.ts, src/lib/import/parse.worker.ts, src/lib/import/__tests__/unzip.test.ts, src/lib/import/__tests__/upsert.test.ts, src/lib/import/__tests__/workerLogic.test.ts</files>
  <read_first>RESEARCH.md Patterns 2, 3, 5 and Pitfalls 2, 5, 6 for the fflate listing op, bomb caps, batched anyOf diff, traversal-safe basenames, and re-parse-on-confirm; src/lib/import/commit.ts and src/lib/db/repositories.ts from 03-01 for the commit path and onProgress hook reused unchanged</read_first>
  <action>Create src/lib/import/unzip.ts importing unzipSync from fflate: export MAX_ZIP_BYTES 25_000_000 compressed cap checked on the File size before inflate and MAX_UNZIPPED_BYTES 50_000_000 per-entry cap throwing zip-entry-too-large, export listTxtEntries returning basename entries skipping directory entries, __MACOSX segments, and dotfiles, export extractTxtFromZip picking the largest .txt by byte length and throwing no-txt-in-zip when none, export sniffZipMagic checking PK-03-04 leading bytes for picker files whose extension lies; chat names derive from basenames only so traversal entries can never influence paths, and entry names are display text only never passed to eval fetch or disk writes. Extend src/lib/import/preview.ts additively with diffPreview taking db plus dedup-hash array plus chatName: batch anyOf queries at 5000 keys per round, collect seen keys into a Set, compute skippedCount by membership and newCount as remainder, look up existingChat via findChatByName, return newCount plus skippedCount plus optional existingChat; no offset and no unbounded reads anywhere. Extend the worker to post progress events during decode and parse phases per the existing WorkerResponse progress variant, keep posting the small preview-result only (stats plus samples plus hashes) and never the full text or full message array. Main-thread zip flow: validate compressed size, arrayBuffer, unzipSync listing on the main thread (millisecond-scale op), transfer the winning entry bytes to the existing worker path unchanged. Confirm path stays re-parse-on-confirm from the retained source bytes per 03-01 — for .zip flows the page retains the winning entry Uint8Array (decoded via the same in-worker path on confirm), not the compressed zip Blob (never re-inflate on confirm); note the record-batch fallback in a code comment only if profiling ever shows re-parse jank. Write unzip tests building fixture zips in-test with fflate zipSync: largest-txt wins, __MACOSX plus dirs plus dotfiles ignored, traversal basenames sanitized, corrupt zip throws, no-txt zip throws no-txt-in-zip, over-cap entry throws, magic sniff detects renamed zips. Write upsert tests on fake-indexeddb seeded DBs: all-new import yields newCount N skipped 0, full re-import yields skipped N new 0, partial overlap splits correctly, findChatByName hit returns the row and miss returns undefined, batched diff matches unbatched counts on inputs over one batch. Write workerLogic tests at pure-function level only (no real Worker): decode fallback, buildPreview shape, protocol round-trip construction of progress and preview-result and error variants. Per D-None all thresholds follow RESEARCH.md assumptions; multi-txt zips import largest only with multi-chat import recorded as deferred, and vite-plugin-pwa stays deferred to the PWA phase.</action>
  <verify>
    <automated>bun run test -- src/lib/import/__tests__/unzip.test.ts src/lib/import/__tests__/upsert.test.ts src/lib/import/__tests__/workerLogic.test.ts</automated>
    <automated>bun run typecheck</automated>
    <automated>bunx biome check src/lib/import</automated>
  </verify>
  <done>Zip extraction picks the largest txt with caps and traversal safety, diffPreview returns correct new-vs-skipped counts in bounded batches, worker posts progress plus small preview payloads, all new tests green.</done>
  <acceptance_criteria>All unzip edge cases pass including corrupt, no-txt, over-cap, and traversal fixtures; diffPreview counts verified against seeded DBs for all-new, full-dup, and partial overlap plus batch-boundary inputs; workerLogic covers decode fallback and all three protocol variants; bun run typecheck and bunx biome check exit 0 (typecheck covers lib/worker TS; .svelte covered by build). Covers IMPR-02 (zip accept) and IMPR-05 (new-vs-skipped diff).</acceptance_criteria>
</task>

<task type="auto">
  <name>Component extraction plus progress bar plus error states plus state machine tests</name>
  <files>src/lib/components/DropZone.svelte, src/lib/components/PreviewCard.svelte, src/lib/components/ProgressBar.svelte, src/routes/+page.svelte, src/lib/import/__tests__/importState.test.ts</files>
  <read_first>src/routes/+page.svelte from 03-01 for the inline drop, preview, and progress markup being extracted; RESEARCH.md Pattern 4 sketch and Pitfalls 1, 4 for the synchronous spinner rule, SSR-safe lazy worker, and input reset behavior</read_first>
  <action>Extract three thin Svelte 5 runes components with zero business logic (all rules stay in src/lib/import): DropZone.svelte owns the dragover dragenter preventDefault handling, dragging highlight state, drop reading DataTransfer files, hidden file input with accept .txt,.zip,.TXT,.ZIP resetting value after each selection, keyboard-accessible role button with aria-label, and a files event carrying File arrays upward; PreviewCard.svelte owns stats display of total, date range formatted from epoch millis, participants list, sample messages rendered with escaped interpolation only, new-vs-skipped counts, editable chatName text input that re-runs diffPreview on rename, Confirm and Cancel buttons, capWarning banner when present; ProgressBar.svelte owns determinate rendering from done-over-total plus indeterminate mode for the sub-100ms spinner phase, with role progressbar and aria-valuenow attributes. Rewire +page.svelte as the orchestrator: status state machine idle reading parsing preview committing done error, status set synchronously in the same tick as drop or change before any await, worker progress events driving ProgressBar through parsing, commitImport onProgress driving it through committing, zip branch showing an unzipping status note before worker handoff, error branch mapping unsupported-type, empty-file, file-too-large, no-txt-in-zip, zip-entry-too-large, and worker error codes to distinct user-facing messages with zero writes in every failure path, cancel terminating the worker and resetting to idle with zero writes, success showing chat name plus written count plus a reset control. Write importState tests against a framework-free status-transition helper co-located with the page logic (extract the transition function so tests import it without Svelte): files-received sets reading synchronously, worker first-progress moves to parsing, preview-result moves to preview, confirm moves to committing, commit resolve moves to done, any error code moves to error with the right message key, cancel from preview or parsing returns to idle with empty preview. Keep components free of at-html, free of module-scope Worker or File references, and free of duplicated validation or diff logic.</action>
  <verify>
    <automated>bun run test -- src/lib/import/__tests__/importState.test.ts</automated>
    <automated>bun run test</automated>
    <automated>bun run typecheck</automated>
    <automated>bunx biome check src/lib/components src/routes src/lib/import</automated>
    <automated>bun run build</automated>
  </verify>
  <done>Components extracted with progress and error states wired, state machine transitions tested, full suite plus lint plus static build green.</done>
  <acceptance_criteria>importState tests prove synchronous reading transition, parse-to-preview-to-commit-to-done flow, per-error-code messages, and cancel-to-idle with discarded preview; full bun run test passes; bun run typecheck, biome check, and bun run build all exit 0 (typecheck covers lib/worker TS; .svelte covered by build); manual browser pass confirms drop spinner under 100ms, determinate bar during parse and store, editable chat name re-running the diff, and cancel leaving the DB untouched. Covers IMPR-03 (full preview with samples plus counts), SC spinner, and SC progress bar.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zip entry names to chat-name derivation | Attacker-controlled archive paths cross here |
| untrusted zip bytes to inflate | Compressed bomb payload crosses here |
| message text to PreviewCard samples | Attacker-controlled HTML and JS strings cross here |
| progress events to ProgressBar | Worker-driven numbers cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Denial of service | zip bomb via tiny zip expanding to gigabytes | high | mitigate | 25MB compressed cap before inflate plus 50MB per-entry cap after listing; reject before parse with user-facing error |
| T-03-07 | Tampering | zip path traversal entries | medium | mitigate | Basename-only names; entries never written to disk; names rendered as escaped display text only |
| T-03-08 | Cross-site scripting | preview samples and chat or file names | high | mitigate | Escaped interpolation only in PreviewCard and DropZone; no at-html anywhere on user or chat content |
| T-03-09 | Spoofing | extension-only trust for picker files | medium | mitigate | Triple gate of extension plus zip-magic sniff plus parse-success; failures show errors with zero writes |
| T-03-10 | Denial of service | unbounded anyOf key arrays in diffPreview | medium | mitigate | 5000-key batches per indexed query; no full-store scans or offset reads |
| T-03-SC | Tampering | bun installs | high | mitigate | No new installs in this plan; fflate gated by the 03-01 human checkpoint |
</threat_model>

<verification>
bun run test passes fully; bun run typecheck exits 0; bunx biome check exits 0 on all touched dirs; bun run build exits 0; manual browser pass: .zip drop extracts largest txt with progress, preview shows new-vs-skipped with editable name, confirm commits with determinate bar, cancel writes nothing, corrupt and no-txt zips show errors with zero writes.
</verification>

<success_criteria>
IMPR-02 true: picker and drop accept .zip and extract the largest .txt with caps. IMPR-03 true: preview shows total, date range, participants, samples, plus new-vs-skipped counts before commit. IMPR-05 true: existing chat detected by name with correct new and skipped counts; confirm merges, cancel cleans up structurally. SC spinner true: immediate feedback on drop. SC progress true: determinate bar during parse and store.
</success_criteria>

<output>
Create `.planning/phases/03-import-feature/03-02-SUMMARY.md` when done
</output>
