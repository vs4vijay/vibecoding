---
phase: 04-chat-browsing
plan: '02'
type: execute
wave: 2
depends_on:
- 04-01
files_modified:
- src/lib/chat/windows.ts
- src/lib/components/ChatView.svelte
- src/lib/components/MessageBubble.svelte
- src/lib/components/DateSeparator.svelte
- src/lib/chat/__tests__/windows.test.ts
- src/lib/chat/__tests__/formatting.test.ts
autonomous: true
requirements:
- BROW-02
- BROW-03
- BROW-04
must_haves:
  truths:
  - Chat views with 100K+ messages render at most ~200 DOM nodes regardless of total message count
  - Loading older messages via scroll-to-top keeps the viewport anchored (no teleport jump) and trimmed pages remain re-fetchable by scrolling up again
  - Messages group under day divider rows and consecutive same-sender messages share one bubble header with deterministic sender colors
  - Empty chats, unknown selections, and import-free libraries each render a distinct helpful empty state
  - Full suite plus lint plus static build stay green after virtualization lands
  artifacts:
  - src/lib/chat/windows.ts
  - src/lib/components/ChatView.svelte
  - src/lib/components/DateSeparator.svelte
  - src/lib/components/MessageBubble.svelte
  key_links:
  - ChatView trims rendered pages to the newest 3 and recomputes the cursor from the oldest held message so trimmed history re-fetches without data loss
  - Scroll anchoring uses the scrollHeight-delta formula after a Svelte tick flush, never scrollIntoView on prepend
  - One shared IntersectionObserver per ChatView drives older-page loads; zero per-bubble observers
---

<objective>
Expand the 04-01 tracer into the full browsing experience: paged-window virtualization with a hard ~200-node DOM budget, scroll-anchored older-page loading, day dividers with sender grouping and colors, complete empty states, and full test coverage — closing every remaining Phase 4 success criterion.

Purpose: Deliver WhatsApp-style browsing that stays smooth on 100K-message chats while keeping every helper headless-unit-tested and the static build green.
Output: Virtualized chat view with anchored infinite scroll, grouped bubbles, dividers, empty states, and a green suite plus build.
</objective>

<execution_context>
@/Volumes/Main/GitHub/vibecoding/skills/.opencode/gsd-core/workflows/execute-plan.md
@/Volumes/Main/GitHub/vibecoding/skills/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-chat-browsing/01-RESEARCH.md
@.planning/phases/04-chat-browsing/04-01-SUMMARY.md
@src/lib/chat/windows.ts
@src/lib/chat/formatting.ts
@src/lib/chat/placeholders.ts
@src/lib/db/repositories.ts
@src/lib/components/ChatView.svelte
@src/lib/components/MessageBubble.svelte
</context>

<tasks>

<task type="auto">
  <name>Virtualized windowing plus scroll anchoring in ChatView</name>
  <files>src/lib/chat/windows.ts, src/lib/components/ChatView.svelte, src/lib/chat/__tests__/windows.test.ts</files>
  <read_first>RESEARCH.md Pattern 3 and Pitfalls 1, 2, 6 for the paged-windowing contract, the scrollHeight-delta anchor formula, the flex-col-reverse ban, and the single-observer rule; src/lib/chat/windows.ts from 04-01 for the existing WindowState, cursorOf, prependPage, renderedCount, and assertRenderBudget signatures extended additively; src/lib/db/repositories.ts getOlderPage for the verbatim cursor shape and malformed-cursor-returns-empty guarantee reused as-is</read_first>
  <action>Extend src/lib/chat/windows.ts additively (no existing signature broken): add trimToBudget keeping the newest MAX_RENDERED_PAGES pages, add a pure anchorDelta pure helper computing newScrollTop from old scrollTop plus new minus old scrollHeight, add isSameDay for day grouping, add groupForRender folding a chronological page array into day sections with consecutive same-sender runs flagged showSender false after the first in each run, and keep assertRenderBudget as the test-enforced invariant. Rewrite src/lib/components/ChatView.svelte around the window state machine: initial load via getLatestWindow reversed to chronological with scrollTop pinned to scrollHeight after tick; a 1px top sentinel observed by one shared IntersectionObserver per ChatView instance triggering loadOlder, plus a visible Load older messages button as the accessibility and no-observer fallback that always renders when hasMore is true; loadOlder measures scrollHeight, fetches getOlderPage with the cursorOf cursor, prepends via prependPage (which trims to newest 3), awaits tick from svelte, then applies the anchorDelta correction so the viewport holds position; trimmed pages stay re-fetchable because the cursor recomputes from the oldest held message, so scrolling up again re-reads dropped history with no data loss. Enforce the budget structurally: ChatView asserts renderedCount against the 200 cap in dev via assertRenderBudget, renders chronological top-to-bottom DOM with no column-reverse anywhere, allocates zero per-bubble observers or per-row timers, and reverses each newest-first repository page to chronological before render. Write windows tests pinning the full contract: prepend plus trim keeps newest 3 with cursor recompute pointing at re-fetchable history, empty page sets hasMore false, short page sets hasMore false, same-ms sibling order preserved, anchorDelta arithmetic on grow and no-grow cases, isSameDay midnight boundaries, groupForRender day splits plus sender-run header suppression, renderedCount budget invariant holding for a simulated 100K stack (2500 stacked pages assert at most 120 rendered), assertRenderBudget throwing over cap. No new dependencies; TanStack virtual stays deferred per the RESEARCH.md audit (hand-rolled paging satisfies the criterion).</action>
  <verify>
    <automated>bun run test -- src/lib/chat/__tests__/windows.test.ts</automated>
    <automated>bun run typecheck</automated>
    <automated>bunx biome check src/lib/chat src/lib/components</automated>
  </verify>
  <done>ChatView loads older pages on scroll-to-top with anchored prepend, trims to 3 rendered pages, keeps a manual load fallback, and all window math passes headless with the budget invariant pinned.</done>
  <acceptance_criteria>All window cases pass including trim-then-refetch cursor correctness, anchor math, day grouping, sender-run suppression, and the simulated-100K budget assertion; no column-reverse in ChatView; exactly one IntersectionObserver per view; bun run typecheck and bunx biome check exit 0. Covers SC-4 (200-node cap with 100K support).</acceptance_criteria>
</task>

<task type="auto">
  <name>Date dividers plus sender colors plus empty states plus phase-green suite</name>
  <files>src/lib/components/DateSeparator.svelte, src/lib/components/MessageBubble.svelte, src/lib/components/ChatView.svelte, src/lib/components/ChatSidebar.svelte, src/routes/+page.svelte, src/lib/chat/__tests__/formatting.test.ts</files>
  <read_first>src/lib/chat/formatting.ts from 04-01 for senderColor, formatClock, and formatDayLabel signatures consumed verbatim; RESEARCH.md Pattern 4 and Pitfall 7 for the sender-always-show default and the every-color-gets-a-dark-variant review rule; src/routes/+page.svelte from 04-01 for the Browse and Import tab shell and ?chat= selection flow extended with empty states</read_first>
  <action>Create src/lib/components/DateSeparator.svelte as a centered day-divider row taking a preformatted label string (formatting stays in formatting.ts, never in the component) with light and dark variants. Upgrade src/lib/components/MessageBubble.svelte: group-chat sender label colored via senderColor with a dark-mode-suffixed palette entry, clock time via formatClock right-aligned in the bubble, consecutive same-sender runs hiding repeat headers per the groupForRender showSender flag, media bodies delegating to MediaPlaceholder, event kinds staying centered rows, all text escaped interpolation only with no at-html. Wire ChatView to render groupForRender sections (DateSeparator per day section, MessageBubble per message) so separators fold into bubble groups and worst-case node count holds near 130 to 160 rows. Add the three empty states: library-empty (zero chats, shown on Browse when the sidebar is empty, pointing at the Import tab), chat-empty (zero messages in a selected chat), and unknown-selection (bad ?chat= id, offering a return to the newest chat); each as plain markup in ChatView or +page.svelte with no new stores. Extend formatting tests only for grouping-adjacent pure behavior (senderColor determinism across calls, day-label boundaries already covered in 04-01 stay passing). Finish with the phase gate: full bun run test, bun run typecheck, bunx biome check on all touched dirs, and bun run build proving no prerender IndexedDB access; fix any fallout in place. Manual UAT checklist for the summary: seed a large chat and count document nodes matching data-msg staying under 200 after repeated older-page loads, scroll-anchor feel with no jump, dark toggle times system switching across all new surfaces, ?chat= deep link, and build output serving.</action>
  <verify>
    <automated>bun run test -- src/lib/chat/__tests__/formatting.test.ts</automated>
    <automated>bun run test</automated>
    <automated>bun run typecheck</automated>
    <automated>bunx biome check src/lib/chat src/lib/components src/routes</automated>
    <automated>bun run build</automated>
  </verify>
  <done>Bubbles show colored senders with day dividers and grouped headers, all empty states render, and the full suite plus lint plus static build are green.</done>
  <acceptance_criteria>Divider per calendar day with no duplicates, sender color stable per sender across reloads, repeat headers suppressed inside same-sender runs, all three empty states reachable by manual pass, every color utility in new markup paired with a dark variant, full bun run test plus typecheck plus biome plus build all exit 0 (typecheck covers lib TS; .svelte covered by build). Covers BROW-02 and BROW-03 fully (grouped bubble layout), SC-4 node budget structurally, SC-5 across all surfaces.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export text to grouped bubble render | Attacker-craftable strings cross here on every row |
| scroll observer callbacks to Dexie reads | DOM-driven events trigger cursor reads here |
| day labels to divider rows | Derived display strings cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Cross-site scripting | grouped bubbles, dividers, sender labels | high | mitigate | Escaped interpolation only maintained through grouping refactor; no at-html introduced; review gate greps for at-html before merge |
| T-04-08 | Denial of service | observer-driven page fan-out on fast scroll | medium | mitigate | loading plus hasMore guards serialize fetches; trimmed pages bound memory; cursor recompute prevents duplicate re-read loops |
| T-04-09 | Denial of service | unbounded rendered rows without trim | high | mitigate | MAX_RENDERED_PAGES trim enforced in prependPage with assertRenderBudget dev assertion and a pinned simulated-100K unit test |
| T-04-10 | Tampering | cursor values derived from stored rows | low | mitigate | Cursors come only from repository-returned records; malformed cursors return empty per the existing getOlderPage guard, never a scan |
| T-04-SC | Tampering | bun installs | high | mitigate | No installs in this plan; TanStack virtual and date-fns stay deferred per the RESEARCH.md audit |

</threat_model>

<verification>
bun run test passes fully; bun run typecheck exits 0; bunx biome check exits 0 on all touched dirs; bun run build exits 0; manual browser pass: large seeded chat stays under 200 message nodes across repeated older-page loads, no scroll jump on prepend, day dividers plus sender colors render in both themes, all three empty states reachable, ?chat= deep link loads directly.
</verification>

<success_criteria>
BROW-02 true: full bubble layout with grouping. BROW-03 true: sender colors, timestamps, day dividers in WhatsApp-style rows. BROW-04 true (held): placeholders intact through the grouping refactor. SC-4 true: rendered nodes under ~200 regardless of total with 100K support. SC-5 true: all surfaces themed. Phase gate green.
</success_criteria>

<output>
Create `.planning/phases/04-chat-browsing/04-02-SUMMARY.md` when done
</output>
